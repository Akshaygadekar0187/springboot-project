package stranger.com.entities;
import java.awt.Color;
import java.awt.*;

/*public class Vecna {

    public int x, y;
    public int WIDTH = 60, HEIGHT = 80;

    public int hp = 150;
    public int maxHp = 150;

    public boolean isActive = false;

    public Vecna(int x, int y) {
        this.x = x;
        this.y = y;
    }

    public void spawn() {
        isActive = true;
        hp = maxHp;
        y = 20;  // top area
    }

    public void update() {
        if (!isActive) return;

        y += 1;

        // Return to Upside Down
        if (y > 250) {   
            y = 20;
        }
    }

    public void draw(Graphics g) {
        g.setColor(Color.red);
        g.fillRect(x, y, WIDTH, HEIGHT);
    }
}
*/